{
  "name": "loopcast-render-service",
  "version": "1.0.0",
  "description": "FFmpeg-based video rendering service for Loopcast — replaces Shotstack",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "express": "^4.19.0"
  }
}